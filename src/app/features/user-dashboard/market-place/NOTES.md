# Platform Icon Component Template

Since we need to dynamically render different SVG icons based on platform, I'll create a helper component. However, for now, let me provide a simpler solution using conditional rendering in the template.

## Solution

Create a shared component or use `@switch` in the template to render platform-specific icons and gradients.
